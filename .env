# GPU Configuration
NVIDIA_VISIBLE_DEVICES=all  # Use 'all' to allow access to all GPUs, or a specific GPU (e.g., 0, 1).
CUDA_VISIBLE_DEVICES=0  # Set which GPUs are visible to the container.

# NVIDIA Capabilities
NVIDIA_DRIVER_CAPABILITIES=compute,utility  # Enable compute and utility features (e.g., CUDA, nvidia-smi).

# Add any other environment-specific variables below as needed